<!DOCTYPE html>
<html>
    <head>
        <title>Random Beer Page</title>
        <link rel="stylesheet" href="/styles.css">
    </head>

<body>
    <h1>Random Beer Page</h1>
    <div class="beer-details">
        <img src="{{beer.image_url}}" alt="{{beer.name}}" style="max-height: 200px;">
        <h2>{{beer.name}}</h2>
        <p><strong>Tagline:</strong>{{beer.tagline}}</p> 
        <p><strong>Description:</strong>{{beer.description}}</p>
        <p><strong>Food Pairing:</strong></p>
        <ul>
            {{#each beer.food_pairing}}
            <li>{{this}}</li>
            {{/each}}
        </ul>
        <p><strong>Brew Tips:</strong> {{beer.brewers_tips}} </p>
    </div>
</body>
</html>



{{!-- <div>
    {{#each beer}}

    {{> beerpartials}}

    <p>{{this.food_pairing}}</p>
    <p>{{this.brewer_tips}}</p>

    {{/each}}
</div> --}}
